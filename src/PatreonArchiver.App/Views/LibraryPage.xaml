<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="PatreonArchiver.App.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:PatreonArchiver.App.Views"
    xmlns:vm="using:PatreonArchiver.App.ViewModels"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Library" />

        <Grid Grid.Row="1">
            <TextBlock
                MaxWidth="420"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextAlignment="Center"
                TextWrapping="Wrap"
                Text="まだアーカイブされた動画はありません。Browse で取り込み、Sync で取得してください。"
                Visibility="{x:Bind views:LibraryPage.VisibleIfTrue(ViewModel.IsEmpty), Mode=OneWay}" />

            <ItemsView
                AutomationProperties.AutomationId="LibraryGrid"
                ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                Visibility="{x:Bind views:LibraryPage.VisibleIfFalse(ViewModel.IsEmpty), Mode=OneWay}">
                <ItemsView.Layout>
                    <UniformGridLayout
                        ItemsStretch="Fill"
                        MinColumnSpacing="12"
                        MinItemWidth="240"
                        MinRowSpacing="12" />
                </ItemsView.Layout>
                <ItemsView.ItemTemplate>
                    <DataTemplate x:DataType="vm:LibraryItemViewModel">
                        <ItemContainer AutomationProperties.Name="{x:Bind Title}">
                            <Border
                                Padding="16"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource OverlayCornerRadius}">
                                <StackPanel Spacing="6">
                                    <FontIcon
                                        HorizontalAlignment="Left"
                                        FontSize="28"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE714;" />
                                    <TextBlock
                                        MaxLines="2"
                                        Style="{StaticResource BodyStrongTextBlockStyle}"
                                        Text="{x:Bind Title}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="Wrap" />
                                    <TextBlock
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Text="{x:Bind Subtitle}" />
                                    <StackPanel
                                        Margin="0,4,0,0"
                                        Orientation="Horizontal"
                                        Spacing="8">
                                        <HyperlinkButton Command="{x:Bind PlayCommand}" Content="再生" />
                                        <HyperlinkButton Command="{x:Bind OpenFolderCommand}" Content="フォルダ" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </ItemContainer>
                    </DataTemplate>
                </ItemsView.ItemTemplate>
            </ItemsView>
        </Grid>
    </Grid>
</Page>
