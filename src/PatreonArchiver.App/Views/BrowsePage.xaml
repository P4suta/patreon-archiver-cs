<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="PatreonArchiver.App.Views.BrowsePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar
            Grid.Row="0"
            HorizontalAlignment="Left"
            Background="Transparent"
            DefaultLabelPosition="Right">
            <AppBarButton
                AutomationProperties.AutomationId="BrowseBack"
                Click="Back_Click"
                Icon="Back"
                Label="戻る" />
            <AppBarButton
                AutomationProperties.AutomationId="BrowseForward"
                Click="Forward_Click"
                Icon="Forward"
                Label="進む" />
            <AppBarButton
                AutomationProperties.AutomationId="BrowseReload"
                Click="Reload_Click"
                Icon="Refresh"
                Label="再読み込み" />
            <AppBarSeparator />
            <AppBarButton
                AutomationProperties.AutomationId="BrowseCapture"
                Click="Capture_Click"
                Icon="Camera"
                Label="このページを取得" />
            <AppBarButton
                AutomationProperties.AutomationId="BrowseExportCookies"
                Click="ExportCookies_Click"
                Icon="Contact"
                Label="Cookie を書き出し" />
        </CommandBar>

        <InfoBar
            Grid.Row="1"
            Title="{x:Bind ViewModel.StatusTitle, Mode=OneWay}"
            IsOpen="{x:Bind ViewModel.HasStatus, Mode=TwoWay}"
            Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
            Severity="{x:Bind ViewModel.StatusSeverity, Mode=OneWay}" />

        <WebView2
            x:Name="Web"
            Grid.Row="2"
            AutomationProperties.AutomationId="PatreonWebView" />
    </Grid>
</Page>
